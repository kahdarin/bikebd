<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.curd.dao.create.CreateUserDao">

    <insert id="create" parameterType="map">
        INSERT INTO bike.company_user(user_id, user_name, password, authority, phone_number)
        VALUES (#{user_id}, #{user_name}, #{password}, #{authority}, #{phone_number})
    </insert>

</mapper>